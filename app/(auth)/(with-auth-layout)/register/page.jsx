//ROUTE GROUP LAYOUT -   Allows multiple pages within the project to be linked to one layout. In this case, adding a (with-auth-layout) folder and adding the other folders into it allows those pages to be linked.
import React from "react";

const page = () => {
  return (
    <div>
      <h1>This is the Registration page</h1>
    </div>
  );
};

export default page;
